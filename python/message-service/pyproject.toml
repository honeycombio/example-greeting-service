[tool.poetry]
name = "message-service"
version = "0.1.0"
description = ""
authors = ["Douglas Soo <doug@honeycomb.io>"]

[tool.poetry.dependencies]
python = "^3.8"
bottle = "^0.12.18"
honeycomb-beeline = "^3.4.0"

[tool.poetry.dev-dependencies]
pytest = "^5.2"

[build-system]
requires = ["poetry>=0.12"]
build-backend = "poetry.masonry.api"
