apiVersion: v1
kind: Service
metadata:
  name: frontend
spec:
  type: NodePort
  selector:
    app: frontend
    # language: go
  ports:
    - nodePort: 30000
      port: 7000
      targetPort: 7000
      protocol: TCP
