apiVersion: apps/v1
kind: Deployment
metadata:
  name: message-ruby
  labels:
    app: message
    language: ruby
spec:
  selector:
    matchLabels:
      app: message
      language: ruby
  template:
    metadata:
      labels:
        app: message
        language: ruby
    spec:
      containers:
      - name: message-ruby
        image: hnyexample/message-ruby
        ports:
        - containerPort: 9000
        env:
        - name: HONEYCOMB_API_KEY
          valueFrom:
            configMapKeyRef:
              name: hny-config
              key: api_key
        - name: HONEYCOMB_DATASET
          valueFrom:
            configMapKeyRef:
              name: hny-config
              key: dataset
        - name: SERVICE_NAME
          value: message-ruby
